#include "obarray.h"

#include "sicp-std.h"

obarray obarray_new(size_t initial_capacity)
{
	unused(initial_capacity);
	return NULL;
}

char *obarray_intern(char *string)
{
	unused(string);
	return NULL;
}

void obarray_free(obarray obarray)
{
	unused(obarray);
}
